# PostgreSQL Database Configuration (for Docker)
POSTGRES_DB=prompt_builder
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# Backend Database Connection (should match PostgreSQL settings above)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=prompt_builder
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-very-secure-secret-key-change-this-in-production

# Frontend Configuration (for Docker)
VITE_BACKEND_URL=http://localhost:5001

# Optional: Override with full DATABASE_URL if needed
# DATABASE_URL=postgresql://postgres:password@localhost:5432/prompt_builder

# Docker-specific overrides (when running with docker-compose)
# DB_HOST=db (uncomment when using Docker)
# VITE_BACKEND_URL=http://backend:5000 (uncomment when using Docker)
